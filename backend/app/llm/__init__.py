"""backend/app/llm — LLM explanation and translation layer.

Modules:
    explanation_generator  — IBM Granite client + offline f-string fallback
    translation            — language routing (en / hi / both)
"""
