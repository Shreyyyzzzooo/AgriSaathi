"""backend/app/api/routes — FastAPI route handlers.

Modules:
    farmer_input  — POST /api/farmer
    crops         — GET  /api/crops
    simulation    — POST /api/simulate
    explain       — POST /api/explain
"""
